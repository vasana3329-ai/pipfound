# بازبینیِ حرفه‌ایِ pipfound — ایرادها، کمبودها، و راهِ به‌روزرسانی/نصب

> این سند از **خواندنِ خودِ کد** و **آزمونِ زندهٔ موتور** نوشته شده، نه از روی حرفِ رابط.
> هر ایراد شاهد دارد (`فایل:خط`) و هر ادعایی که با اجرا سنجیده شده با «سنجیده شد» علامت خورده.
> روزِ بازبینی: ۲۰۲۶-۰۹-۱۹ (شنبه — بازار فارکس بسته) · `main` = `0b16548`
>
> ### 🔄 به‌روزرسانیِ همین روز (پس از نوشتنِ این سند)
> **P0-1 انجام شد** (تحلیل فقط روی کندلِ بسته + سنِ داده/باز-بسته در API و رابط + گیتِ درجه +
> حذفِ look-ahead بک‌تست؛ تستِ `data_freshness_test.py` با ۵۰ بررسی). پس این موارد را در سند
> دیگر «باز» حساب نکن: B1، B2-بکتست (نگاه به آینده)، و بخشِ «اپ روی بازارِ بسته پلن می‌دهد».
> هنوز بازها: **P0-2 (مدلِ ریسک/سایز)**، P0-3 (هم‌ترازیِ ۴ساعته)، P1-1 (اسپرد/کمیسیون)، P1-2 (گیتِ خبرِ
> زمانِ کندل)، P1-3 (تستِ عددیِ موتور)، P1-4/P1-5، و همهٔ P2/P3. وضعیتِ زندهٔ هر مورد در
> `PIPFOUND_BLUEPRINT.md` بخشِ ۶ (ردیف‌های D1…D12) نگه‌داری می‌شود.
> حجمِ کد: `app.py` ۲۸۵۵ خط (CSS/JS/HTML درونش) · `smc_engine.py` ۸۳۱ · `confluence.py` ۶۶۱ · `backtest.py` ۶۰۱

---

## ۰) خلاصهٔ اجرایی (اگر فقط ۱۰ خط می‌خوانی)

۱. **موتور از نظرِ مفاهیمِ ICT جدی و بالغ است** (BOS/CHoCH/MSS با ماشینِ حالت، FVG با شرطِ دیسپلیسمنت، OB با شرطِ FVG + عدمِ میتیگیشن، سوئیپ، توالیِ sweep→MSS، PD/OTE، کیل‌زون با DST، گیتِ خبر). این بخش بالاتر از میانگینِ ابزارهای «ICT» بازار است.
۲. **اما سه چیز آن را از یک ابزارِ قابلِ‌معامله جدا می‌کند:** (الف) تحلیل روی **کندلِ ناقصِ زنده** انجام می‌شود (repaint)، (ب) **مدلِ ریسک/سایزِ پوزیشن وجود ندارد**، (ج) **بسته‌بودنِ بازار و سنِ داده دیده نمی‌شود** — و همین حالا (شنبه) اپ دارد روی دادهٔ بستهٔ جمعه پلنِ «قابلِ اجرا» می‌سازد.
۳. **۵۶ بررسیِ تست فقط «وجودِ کلید و مسیر» را می‌سنجد، نه درستیِ عدد.** هیچ تستِ طلایی برای سوینگ/ساختار/FVG نیست، پس یک رگرسیونِ ریاضی می‌تواند بی‌صدا رد شود.
۴. **برای نصب روی «هر دستگاه» الان دو دیوارِ سخت وجود دارد:** سرور فقط `127.0.0.1` گوش می‌دهد (`app.py:2798`) و **هیچ پشتیبانیِ TLS ندارد** — و PWA/سرویس‌ورکر روی مبدأِ ناامن نصب نمی‌شود. پس «نسخهٔ نصب‌شدنی روی هر دستگاه» بدونِ HTTPS یا بدونِ بسته‌بندیِ بومی (Tauri/Capacitor) عملاً شدنی نیست.
۵. **هیچ احرازِ هویتی نیست** (`app.py`: جست‌وجوی auth/password/token خالی است). یعنی همان لحظه که `--host 0.0.0.0` بدهی، ژورنالِ معاملات و توانِ اجرای تحلیل روی شبکه باز می‌شود.
۶. **بازهٔ زمانیِ چهارتاییِ سبک‌ها واقعاً استفاده نمی‌شود:** `score()` فقط `tfs[0]`، `tfs[1]` و `tfs[-1]` را می‌بیند (`confluence.py:98-99`)؛ تایم‌فریمِ میانیِ هر سبک تزئینی است.
۷. **«۴ ساعته» روی فارکس/طلا/اندیکس جعلی است:** Yahoo دادهٔ ۴ساعته ندارد، پس از ۱ساعته بازنمونه‌گیری می‌شود و برش‌ها **از ابتدای آرایه** شمرده می‌شوند، نه از مرزِ ساعت. سنجیده شد: **۲۲۸ از ۳۰۰ کندل روی شبکهٔ ۰۰/۰۴/۰۸/۱۲/۱۶/۲۰ UTC نیستند** (فاز ۲ ساعت جابه‌جا) — یعنی دیلینگ‌رنج و OTE روی چارتی لنگر می‌خورد که با چارتِ کارگزار یکی نیست.
۸. **فِرَکتالِ سوینگ `n=2` و تولرانس‌های نسبتیِ ثابت** (`tol=0.0007` برای equal-high و `0.0015` برای الگو) با نوسان مقیاس نمی‌خورند: روی `USDJPY` ≈ ۱۰ پیپ و روی `NAS100` ≈ ۳۰ پوینت «هم‌سطح» حساب می‌شود.
۹. **صفر واقع‌گراییِ اجرا:** نه اسپرد، نه کمیسیون، نه سوآپ، نه اسلیپیج — در کلِ `backtest.py` و `app.py` (جست‌وجو خالی). برای اسکالپِ ۵m/۱m این خطا بزرگ‌ترین عاملِ فریبِ RR است.
۱۰. **بک‌تست با ساعتِ دیوار آلوده می‌شود:** گیتِ خبر از `now_et()` می‌آید (`macro_context.py:168`) نه از تایم‌استمپِ کندل، و آفستِ ET را `-4` ثابت گرفته (بدونِ DST) — پس بک‌تست ناتکرارپذیر است و در زمستان پنجره‌ها یک ساعت جابه‌جا می‌شوند.

---

## ۱) ایرادهای درستیِ فنی (Bug) — با شاهد

| # | شدت | ایراد | شاهد |
|---|---|---|---|
| B1 | 🔴 P0 | **کندلِ ناقص در تحلیل**: `fetch_binance`/`fetch_yahoo` کندلِ در حالِ تشکیل را هم برمی‌گردانند و `analyze_bars` از `bars[-1]` برای `last_price`، سوینگ، FVG، OB، PD و حتی کیل‌زون استفاده می‌کند. نتیجه: پلن تا لحظهٔ بسته‌شدنِ کندل عوض می‌شود (repaint) و بک‌تست (که فقط کندلِ بسته می‌بیند) توزیعِ رفتاریِ زنده را هرگز اعتبارسنجی نکرده. | `smc_engine.py:116-172,768-800`؛ سنجیده شد: آخرین کندلِ ۱۵m با سنِ ۹۱۵ دقیقه = کندلِ بستهٔ جمعه، ولی هیچ جا «بسته‌بودن» بررسی نمی‌شود |
| B2 | 🔴 P0 | **بازنمونه‌گیریِ ۴ساعته ناهم‌تراز**: `resample` گروه‌ها را `range(0, len, n)` می‌چیند؛ هم فاز را از ابتدای پنجره می‌گیرد هم برشِ ناقصِ آخر را حذف می‌کند. | `smc_engine.py:173-182`؛ سنجیده شد: ۲۲۸/۳۰۰ کندل خارجِ شبکهٔ ۴ساعته، اولین کندل ۱۰:۰۰ UTC با فاز `hour%4=2` |
| B3 | 🟠 P1 | **کیل‌زونِ London Close عملاً مرده است**: بررسی‌ها به ترتیب‌اند و بازه‌ها هم‌پوشانی دارند؛ `NY AM (8.5-11)` همیشه `London Close (10-12)` را در ۱۰:۰۰–۱۱:۰۰ می‌بلعد. | `smc_engine.py:711-716`؛ سنجیده شد: ET 10:30 → `New York AM KZ` (نه London Close) |
| B4 | 🟠 P1 | **برچسبِ اشتباهِ سشنِ آسیا**: `19<=h<24 or 0<=h<2` یک برچسب دارد، پس ساعتِ ۰۰:۳۰ ET با برچسبِ «۱۹:۰۰–۲۴:۰۰» گزارش می‌شود. | همان‌جا؛ سنجیده شد: ET 00:30 → `Asian Range (19:00-24:00 ET)` |
| B5 | 🟠 P1 | **گیتِ خبر با ساعتِ دیوار و آفستِ ثابتِ ۴-**: در بک‌تست از زمانِ اجرا استفاده می‌کند (نه زمانِ کندل) و در زمستان یک ساعت جابه‌جا می‌شود. | `macro_context.py:168-188` (`now_et()`، `timedelta(hours=-4)`) در برابرِ DSTِ درستِ `smc_engine._is_us_dst` |
| B6 | 🟠 P1 | **تایم‌فریمِ میانی بی‌استفاده**: استک‌های چهارتایی اعلام می‌شوند ولی گریدر فقط اول/دوم/آخر را می‌خواند؛ ۱۵m در اسکالپ و ۱h در روزانه تنها در نمایشِ بایاس ظاهر می‌شوند. | `app.py:210-217` در برابرِ `confluence.py:98-99` |
| B7 | 🟠 P1 | **سطوحِ روزانه روی تعریفِ اشتباهِ سشن برای اندیکس/استاک**: `_session_levels` از روزِ تقویمیِ ET استفاده می‌کند، ولی سشنِ آتیِ اندیکس‌ها ۱۸:۰۰ ET باز می‌شود؛ ضمناً برای `1d/1w` کامل کنار گذاشته می‌شود. | `smc_engine.py:425-462` (`if tf in ("1d","1w"): return {}`) |
| B8 | 🟡 P2 | **شیفتِ بیسیسِ فلزات روی کلِ تاریخچه**: سریِ آتیِ COMEX با اختلافِ لحظه‌ایِ اسپات جابه‌جا می‌شود؛ با تغییرِ بیسیس، همهٔ سطوحِ تاریخی به‌طورِ یکنواخت جابه‌جا می‌شوند و با چارتِ اسپات کارگزار نمی‌خوانند. | `smc_engine.py:183-196` |
| B9 | 🟡 P2 | **شمارشِ معاملاتِ ناتمام**: معامله‌ای که در افقِ `max_hold` نه استاپ نه هدف را نزده، `None` برمی‌گردد و **کلاً از آمار حذف می‌شود** → سوگیریِ آماری (اگر همان‌ها ضرر می‌کردند، وین‌ریت خوش‌بینانه می‌شود). | `backtest.py:226` |
| B10 | 🟡 P2 | **توابعِ O(n²)**: `_break_displaced` از هر سوینگ تا انتهای سری جلو می‌رود و `sweeps` برای هر کندل روی همهٔ سوینگ‌های قبلی می‌گردد؛ در `limit=300` مشکلی نیست، در بک‌تستِ سنگین/موبایل دردسر می‌شود. | `smc_engine.py:218-246,627-654` |
| B11 | 🟡 P2 | **بدونِ کشِ کندل/بک‌آف**: هر تحلیل ۳ تا ۴ تایم‌فریم را جدا دانلود می‌کند و کارگرِ آلارم هر ۹۰ ثانیه به `_live_price` می‌زند. محدودیتِ نرخ خبری نیست (همین امروز یک کاوشِ دستی برای خودم ۴۲۹ گرفت). | `app.py:423-467`، `smc_engine.py:72-76` (بدونِ retry/backoff) |
| B12 | 🟡 P2 | **خطای شبکه به‌شکلِ خام به کاربر می‌رسد** (JSON خام) به‌جای پیامِ انسانی + دلیلِ قابلِ‌فهم. | مسیرِ `/api/analyze` در `app.py` |

---

## ۲) کمبودهای متدولوژی ICT/SMC (از دیدِ یک معامله‌گر)

**۲.۱ چارچوبِ «نقدینگی و هدف» ناقص است.**
PDH/PDL و رِنجِ آسیا هست (`smc_engine.py:425`)، اما نبودِ اینها یعنی «Draw on Liquidity» واقعی محاسبه نمی‌شود:

- **PWH/PWL** (سقف/کفِ هفتهٔ قبل)، **PMH/PML** (ماهِ قبل) — جست‌وجو خالی است.
- **IPDA ۲۰/۴۰/۶۰ روزه** و اهدافِ HTF — وجود ندارد.
- **NDOG/NWOG** (گپِ بازگشاییِ روز/هفتهٔ نو) — وجود ندارد.
- **اوپنِ نیمه‌شب (۰۰:۰۰ ET)** که در مکتبِ ICT لنگرِ بایاسِ روز است — وجود ندارد.
- بازه‌های ۸:۳۰ و ۹:۳۰ ET (اوپنِ سهام/داده) — وجود ندارد.

**۲.۲ SMT و تأییدِ بین‌بازاری، فقط متنِ آموزشی است.**
`CORRELATION_CLUSTERS` و جملهٔ «اگر ۳ هم‌جهت بودند و یکی عقب ماند → SMT» **رشتهٔ ثابت** است (`macro_context.py:100-118`) و هیچ‌جا محاسبه نمی‌شود. «جوداس سوئینگ» هم فقط متن است (`macro_context.py:69-85`). یعنی هیچ تأییدِ بیرونی (DXY، US10Y، ES/NQ در برابرِ YM، طلا در برابرِ نقره) در امتیاز دخالت نمی‌کند.

**۲.۳ مدل‌های زمانیِ ICT پیاده نشده‌اند.** Power of 3 / AMD، Turtle Soup، اوپنِ رِنج (ORB)، و به‌ویژه **MACROهای زمانی** (مثل ۹:۵۰–۱۰:۱۰) — هیچ‌کدام نیستند. تنها مدلِ زمانی، پنجرهٔ سیلوربولت است که خودتان بر پایهٔ یک اندازه‌گیریِ کوچک (`kz_measure.py`) به ۹–۱۱ ET گسترش داده‌اید؛ این باید پارامترِ **قابلِ آزمون‌به‌ازایِ هر نماد** شود، نه ثابتِ کد.

**۲.۴ بلاک‌ها تمایز ندارند.** `order_blocks` همه را یک نوع می‌بیند: **breaker block**، **mitigation block**، **rejection block**، و **propulsion/vacuum block** جدا نمی‌شوند و «قدرتِ» OB (تازگی، تعدادِ لمس، اندازهٔ FVGِ زادآور) درجه‌بندی نمی‌شود. FVG هم یک نوع است؛ تمایزِ **BISI/SIBI** و استفاده از **consequent encroachment** (نه فقط مید برای ابطال) وجود ندارد.

**۲.۵ لنگرِ PD روی «رِنجِ کوچک» است.** `premium_discount` روی آخرین پایِ ایمپالس کار می‌کند، نه روی **دیلینگ‌رنجِ سشن**؛ پس «پریمیوم/دیسکانت» به تعریفِ ICT روی رِنجِ معاملاتیِ روز قابلِ استناد نیست. (این را در `confluence.py:120-135` خودتان هم دیده‌اید و با اولویتِ mtf→htf وصله کرده‌اید.)

**۲.۶ تقویمِ معاملاتی وجود ندارد.** نه درجه‌بندیِ «روندی/رِنجیِ» روز، نه ADR/ATR برای سنجشِ «آیا این هدف در دسترسِ امروز است؟»، نه خبرِ NFP/FOMC/OPEX/رول‌اور، نه قاعدهٔ **جمعه‌آخرتخت‌شدن**، نه رفتارِ گپِ یکشنبه.

**۲.۷ مدلِ ریسک صفر است.** 🔴 مهم‌ترین کمبودِ عملی:
- نه سرمایه/درصدِ ریسک → نه **سایزِ پوزیشن** (لات/کانترکت). در ژورنال فقط رشتهٔ ثابتِ `risk_pct="1"` هست (`app.py:2770`).
- نه **سقفِ ضررِ روزانه**، نه **کات‌آفِ بعد از N ضرر**، نه **سقفِ تعدادِ معامله**.
- نه تعدیلِ **همبستگی**: سه شرطِ هم‌جهت روی EURUSD/GBPUSD/XAUUSD در واقع یک شرطِ بزرگ روی دلار است.

**۲.۸ مدیریتِ معامله در پلنِ زنده نیست.** در بک‌تست TP1+BE هست، ولی پلنِ زندهٔ UI فقط یک TP نشان می‌دهد: نه پله‌های مدیریت، نه **سطحِ ابطال**، نه **خروجِ زمانی** («اگر تا X کندل کار نکرد، بیرون»)، نه منطقِ re-entry.

**۲.۹ اعتبارسنجیِ آماری در حدِ نمونهٔ کوچک است.** سندِ خودتان به «میانهٔ planned_rr ~۴۸× و ۳/۱۶ معامله تا ≥۲R» اشاره می‌کند؛ ژورنالِ واقعی روی این ماشین **۳ معامله** است. آستانه‌های درجه (`ratio≥0.85/0.70/0.50/0.30` در `confluence.py:468-478`) دستی‌اند و روی داده کالیبره نشده‌اند. جمعِ اصلاحاتِ C1..C24 روی نمونه‌های کوچک انجام شده → **ریسکِ بیش‌برازش**.

**۲.۱۰ خروجیِ تاپ‌داون زنجیره‌ای نیست.** کاربر نمی‌بیند «برای اجرا، ابتدا باید کدام تأیید روی کدام تایم‌فریم بیاید». فقط یک چک‌لیستِ تخت + یک درجه.

---

## ۳) وضعیتِ آزمون‌ها (چه محافظت‌شده و چه بی‌محافظت)

- ✅ **محافظت‌شده:** وجودِ ۵۳ شناسه/کلید در صفحه (`selfcheck-baseline.json`)، ۲۸ سیم‌کشی، ۲۲ مسیر، تستِ بصریِ هدلس، تستِ ری‌استارتِ خودکار، تستِ رفت‌وبرگشتِ ژورنال، ۵۶ بررسیِ نگهبانِ بازگشت، هوکِ pre-commit، ورک‌فلوی CI + branch protection.
- ❌ **بی‌محافظت (و همان‌جاست که خرابیِ بی‌صدا می‌آید):**
  1. **هیچ تستِ عددی برای موتور نیست.** نه سوینگ/FVG/OBِ طلایی، نه توالیِ sweep→MSS، نه PD/OTE. تغییرِ `disp_mult=1.5` به `1.4` هیچ تستی را نمی‌شکند ولی نتیجهٔ معاملات را عوض می‌کند.
  2. **هیچ تستی برای کندلِ ناقص/بسته‌بودنِ بازار نیست** (B1/B2 همان‌جاست).
  3. **هیچ تستی برای هم‌ترازیِ ۴ساعته نیست** (B2).
  4. **هیچ تستی برای DST/تایم‌زون خبر نیست** (B4/B5).
  5. **هیچ تستی برای دقتِ بک‌تست نیست** (اسپرد/اسلیپیج/معاملاتِ ناتمام).

---

## ۴) نصب روی «هر دستگاه» — وضعیت و راه

### ۴.۱ دیوارهای فعلی (سنجیده شد)
| دیوار | شاهد | نتیجه |
|---|---|---|
| فقط `127.0.0.1` گوش می‌دهد | `app.py:2798` (`default="127.0.0.1"`) | گوشی/تبلت حتی در همان وای‌فای هم به اپ نمی‌رسد |
| **هیچ TLS‌ای نیست** | جست‌وجوی `ssl/cert` = خالی؛ سرور `ThreadingHTTPServer` خام | روی `http://192.168.x.x` مرورگر سرویس‌ورکر نمی‌سازد → **دکمهٔ «نصب» ظاهر نمی‌شود** (شرطِ secure context) |
| هیچ احرازِ هویتی نیست | جست‌وجوی `auth/password/token` = خالی | بازکردن روی شبکه = بازبودنِ ژورنال و اجرای تحلیل برای همه |
| وضعیت (ژورنال/آلارم/اسکرین‌شات) فقط لوکال است | `~/pipfound/…` و `PIPFOUND_JOURNAL_*` | هر دستگاه نصب‌شده، **دفترِ جدا و خالی** دارد |
| مانیفست فقط حداقلی است | `manifest.webmanifest` | نه `id`، نه `screenshots`، نه `shortcuts`؛ `orientation: portrait-primary` برای ابزارِ چارتی غلط است؛ آیکون‌ها ریز (۰٫۹–۴KB) |
| سرویس‌ورکر نسخه‌بندیِ دستی دارد | `sw.js`: `CACHE="pipfound-v1"` | آیکون/مانیفست **کش‌اولِ ابدی** تا ارتقای دستیِ نامِ کش؛ صفحه شبکه‌اول است ولی هیچ بنرِ «نسخهٔ تازه» و هیچ هماهنگیِ `skipWaiting` نیست |

### ۴.۲ سه مسیرِ واقعی (به ترتیبِ هزینه)

**مسیر الف — میزبانیِ سرور با HTTPS + PWA (پیشنهادِ اصلی، کم‌هزینه‌ترین و سریع‌ترین):**
1. یک هدفِ اجرا (`--host 0.0.0.0`) + **TLS** (گزینه‌ها: کانفیگِ `ssl.SSLContext` در خودِ اپ با گواهیِ Let's Encrypt، یا ساده‌تر: گذاشتنِ اپ پشتِ **Cloudflare Tunnel / Tailscale** که خودشان TLS و دسترسی می‌دهند — بدونِ نیاز به IP عمومی).
2. **احرازِ هویتِ سبک**: یک توکنِ محیطی (`PIPFOUND_TOKEN`) که در همهٔ مسیرها و به‌شکلِ هدر/کوکی چک شود (اپ الان API‌محور است، پس ارزان است). PWA روی iOS هم بعد از «Add to Home Screen» کار می‌کند، به‌شرطِ HTTPS.
3. **QR** در ترمینال هنگامِ استارت (خیلی کوچک، ولی همهٔ دردِ «آدرسِ گوشی» را حل می‌کند).
4. تقویتِ مانیفست/سرویس‌ورکر: `id`، `screenshots`، `shortcuts`، حذفِ `orientation`، آیکون‌های درستِ ۵۱۲ و ماسک‌بل، و **نسخه‌بندیِ کش از روی SHAِ بیلد** (تزریق در زمانِ سرو) + بنرِ «نسخهٔ تازه — بارگذاری».

**مسیر ب — بستهٔ قابلِ‌حمل برای ماشینِ خودت (Docker):**
`Dockerfile` چندمعماری + `docker compose up`؛ همان ایمیج روی NAS/سرورِ خانگی/VPS. مزیت: «نصب روی هر دستگاه» = یک ایمیج، و ژورنال/آلارم با mountِ یک پوشه باقی می‌ماند. این بهترین گزینه برای «همه‌جا یک تجربهٔ یکسان» است.

**مسیر ج — اپِ بومی/فروشگاهی (Tauri یا Capacitor) — گران‌ترین:**
برای اینکه روی خودِ گوشی بدونِ سرور اجرا شود، باید موتورِ پایتون را کنارِ اپ بسته‌بندی کنی (PyInstaller + sidecar) که کارِ نگه‌داریِ جدی است. **توصیه: فازِ سوم، نه اول.** مسیرِ الف+ب ۹۵٪ نیاز را با ۱۰٪ هزینه پوشش می‌دهد.

### ۴.۳ وضعیتِ مشترکِ دادهٔ کاربر
هر سه مسیر به این نیاز دارند: **برون‌بری/درون‌بریِ ژورنال، آلارم‌ها، اسکرین‌شات‌ها و تنظیمات** (JSON/ZIP). بدونِ آن، «نصب روی هر دستگاه» یعنی «شروعِ از صفر در هر دستگاه».

---

## ۵) راهِ به‌روزرسانیِ اپ (سه کانال، یک منبعِ نسخه)

**۵.۱ منبعِ واحدِ نسخه:** فایلِ `VERSION` (semver) + SHAیِ گیت که `/api/revision` الان می‌دهد. تگِ `vX.Y.Z` روی هر ادغامِ `main` و فایلِ `CHANGELOG.md`. `/api/version` هم همان‌ها را به‌شکلِ ماشین‌خوان بدهد.

**۵.۲ کانالِ «نسخهٔ لوکالِ گیت» (همین ماشین):**
- نشانگرِ «نسخهٔ تازه موجود است» در چیپِ کنارِ عنوان: مقایسهٔ SHA بارشده با `origin/main` (یک `git fetch --dry-run` سبک یا APIِ گیت‌هاب).
- دکمهٔ «به‌روزرسانی الان» = `git pull --ff-only` → `selfcheck --guard` → ری‌استارتِ خودکارِ موجود. **ماشینِ بازگشت از قبل ساخته شده** (`selfcheck_watcher.py`) و چرخهٔ «خراب شد → برگرد به سبزِ آخر» هم تست‌شده است. پس به‌روزرسانی ایمن است به‌شرطِ: فقط از `localhost` قابلِ‌اجرا باشد، `git pull --ff-only` (بدونِ mergeِ خودکار)، و پیش از ری‌استارت یک اسنپ‌شاتِ `good` بگیرد.
- **مهم:** وضعیتِ «نسخهٔ کهنه» الان فقط برای *کد* دیده می‌شود؛ باید برای **داده** هم اضافه شود (سنِ آخرین کندل + باز/بستهٔ بازار).

**۵.۳ کانالِ «اپِ نصب‌شده» (PWA/موبایل):** نامِ کش از SHA بیلد ساخته شود، در `load` یک‌بار `registration.update()` صدا زده شود، و بنرِ «نسخهٔ تازه آماده است — بارگذاری» با `postMessage`/`skipWaiting` هماهنگ شود. هرگز کش‌اولِ بی‌نسخه.

**۵.۴ کانالِ «کپی‌های دیگر (Docker/VPS)»:** `docker pull ghcr.io/<you>/pipfound:latest` + `compose up -d`؛ برچسبِ نسخه در `LABEL` ایمیج و چکِ نسخه در `/api/version`. (به‌روزرسانیِ خودکار با Watchtower اختیاری.)

**۵.۵ مهاجرتِ داده:** هر نسخه‌ای که شکلِ ژورنال/آلارم را عوض می‌کند، باید یک پلهٔ مهاجرتِ صریح داشته باشد (`journal.csv` فعلاً ۲۲ ستون ثابت دارد). بدونِ آن، نسخهٔ جدید روی دفترِ قدیمی بی‌صدا خراب می‌کند.

---

## ۶) نقشهٔ راهِ پیشنهادی (اولویت‌دار)

| اولویت | کار | چرا اول این | تخمین |
|---|---|---|---|
| P0-1 | تحلیل فقط روی کندلِ **بسته** + نمایشِ «کندلِ جاری ناقص است» و سنِ داده + هشدارِ «بازار بسته» | B1/B2 همان چیزی است که بین «پلنِ زیبا» و «پلنِ اجراشدنی» فرق می‌گذارد؛ تعمیرِ کوچکِ کد با اثرِ بزرگ | ~نیم روز |
| P0-2 | مدلِ ریسک: سرمایه + درصدِ ریسک → **سایزِ پوزیشن**، سقفِ ضررِ روزانه، هشدارِ همبستگی | بدونِ سایز، RR عددی بی‌معناست | ~۱ روز |
| P0-3 | درست‌کردنِ بازنمونه‌گیریِ ۴ساعته (برش بر اساسِ `t // 14400` + دور ریختنِ برشِ ناقصِ آخر) یا استفاده از منبعِ ۴ساعتهٔ درست | B2 روی PD/OTEِ سبکِ روزانه و سوینگ اثرِ مستقیم دارد | ~۲ ساعت |
| P1-1 | اسپرد/کمیسیون/سوآپ + علامت‌زدنِ معاملاتِ ناتمام (به‌جای حذف) در بک‌تست | RR و درجه‌ها را واقعی می‌کند | ~۱ روز |
| P1-2 | گیتِ خبر از تایم‌استمپِ کندل + DSTِ درست + کشِ تاریخیِ تقویم برای بک‌تست | B5 و «بک‌تستِ ناتکرارپذیر» | ~۱ روز |
| P1-3 | تستِ عددیِ طلایی برای موتور (سوینگ/FVG/OB/ساختار/PD) + تستِ «کندلِ ناقص» + تستِ هم‌ترازیِ ۴ساعته | بخشِ ۳: تنها چیزی که جلوی رگرسیونِ بی‌صدا را می‌گیرد | ~۱ روز |
| P1-4 | سطوحِ HTF: PWH/PWL، PMH/PML، اوپنِ نیمه‌شب، NDOG/NWOG + تعریفِ سشنِ درست برای اندیکس/استاک | B7 و «Draw on Liquidity» واقعی | ~۱ روز |
| P1-5 | استفاده از تایم‌فریمِ میانی در امتیاز (یا حذفِ آن از استک) + امتیازِ درجه‌بندی‌شدهٔ OB (تازگی/FVG/دیسپلیسمنت) | B6 و ۲.۴ | ~۱ روز |
| P2-1 | TLS + توکن + `--host` + QR + تقویتِ مانیفست/سرویس‌ورکر → **نصبِ واقعی روی موبایل** | بخشِ ۴ | ~۲ روز |
| P2-2 | Docker + compose + `/api/version` + CHANGELOG + تگ‌های نسخه | بخشِ ۵.۱ و ۵.۴ | ~۱ روز |
| P2-3 | برون‌بری/درون‌بریِ دادهٔ کاربر (ژورنال/آلارم/تنظیمات) | بخشِ ۴.۳ | ~نیم روز |
| P2-4 | تثبیتِ آماری: بک‌تستِ چندنمادی/چندتایم‌فریمی با holdout + کالیبراسیونِ آستانه‌های درجه | ۲.۹ (ریسکِ بیش‌برازش) | ~۳ روز |
| P3 | SMT/تأییدِ بین‌بازاری، Power of 3، مدل‌های زمانی، تفکیکِ breaker/mitigation | ۲.۲ و ۲.۳ | ~۱ هفته |
| P3 | بستهٔ بومیِ Tauri/Capacitor | مسیرِ ج — فقط اگر واقعاً لازم شد | ~۱–۲ هفته |

---

## ۷) آنچه همین حالا سنجیده شد (برای بازتولید)

```
# ۱) کیل‌زون: ET 10:30 → New York AM KZ  (پس London Close در ۱۰–۱۱ هرگز نمی‌آید)
# ۲) برچسبِ آسیا: ET 00:30 → Asian Range (19:00-24:00 ET)
# ۳) ۴ساعتهٔ XAUUSD از Yahoo (GC=F): ۳۰۰ کندل، ۲۲۸ مورد خارجِ شبکهٔ ۴ساعته، فاز hour%4=2
# ۴) سنِ داده در لحظهٔ اجرا: ۱h → ۹۶۰ دقیقه، ۱۵m → ۹۱۵ دقیقه (جمعهٔ بسته) و اپ همان را تحلیل می‌کند
# ۵) ژورنالِ واقعی: ۳ معامله  ·  ```grep -c ssl/cert/auth``` در app.py = صفر
# ۶) tfs[0], tfs[1], tfs[-1] در confluence.py → تایم‌فریمِ میانی بی‌نقش
```
